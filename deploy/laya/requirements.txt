# Match HF Inference Toolkit 0.5.6's Python 3.11 base image.
# torch==2.5.1 accepts the image's installed 2.5.1+cpu build.
# Do not upgrade these independently of the toolkit/torchvision/torchaudio stack.
laya==0.3.20
torch==2.5.1
transformers==4.51.3
huggingface-hub==0.30.2
safetensors==0.8.0
numpy==1.26.4
