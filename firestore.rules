rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the authenticated, allowlisted server repository may access data.
    match /{document=**} { allow read, write: if false; }
  }
}
