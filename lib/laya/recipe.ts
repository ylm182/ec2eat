import type { VerifiedLayaRecipe } from "./provider";
// No deployed HF fixture was supplied. Do not replace this with a guessed /v1/systemone payload.
// Installation requires the sanitized fixture, pinned checkpoint and actual serving runtime.
export const installedRecipe: VerifiedLayaRecipe | null = null;
